spring:
  config:
    activate:
      on-profile: prod
  jpa:
    properties:
      hibernate:
        format_sql: false

logging:
  level:
    root: INFO
    com.arqly: INFO
    org.springframework.web: WARN
    org.hibernate.SQL: WARN
